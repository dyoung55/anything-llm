/**
 * Hook for keyboard navigation when the slash command panel is showing.
 * Stub implementation to support SlashCommands component.
 * @param {{ showing: boolean }} options
 */
export function useSlashCommandKeyboardNavigation({ showing } = {}) {
  // No-op: keyboard navigation can be extended here if needed (e.g. arrow keys, Enter).
  return undefined;
}
